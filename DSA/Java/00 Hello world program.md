
Hello world program:- 
`public class Main {`
    `public static void main(String[] args) {`
        `System.out.println("Hello, Java!");`
    `}`
`}`

In java your code is always inside a class. In java no method or function can float freely it has to belong to an object or argument. 

Lets breakdown the hello world code:-
1)- `public class Main`
Here we create a public class whose object is Main. lets revisit the concept of class and object once again. imagine u want to build a car and this car will have a design and a blueprint which will contain all the stuff a car would have like wheels, engine, door and seats. from this blueprint u will make many cars. a yellow car, a blue car or a red car. these red, yellow and blue cars are objects and ur blueprint is ur class. 
Why use main right now? In cpp we use to have main() function which use to just exist and float. in java we must use the main function to write our main code in it. Make sure that the name of ur main class should be the name of ur file also.
Public is ur access modifier just like u studied in cpp oops. 

2)- `public static void main(String[], args)`
